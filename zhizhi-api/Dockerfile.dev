# 开发环境Dockerfile
FROM node:18-alpine

# 安装开发工具
RUN apk add --no-cache git

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm ci

# 暴露端口
EXPOSE 3000 9229

# 创建日志目录
RUN mkdir -p logs

# 启动应用
CMD ["npm", "run", "dev"]